export type ImagePlaceholder = {
  id: string;
  description: string;
  imageUrl: string;
  imageHint: string;
};

// This file is now empty, but we keep it to avoid breaking imports.
// The data has been removed as per the requirement to remove all profile pictures.
export const PlaceHolderImages: ImagePlaceholder[] = [];
